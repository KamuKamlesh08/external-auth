# Commercetools OAuth Introspect Demo

This project simulates an external OAuth introspection endpoint for Commercetools.

## How to Run

1. Clone the repo
2. Run `npm install`
3. Create `.env` file:
